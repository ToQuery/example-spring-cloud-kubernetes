server:
  port: 8081

management:
  endpoint:
    restart:
      enabled: true
    health:
      enabled: true
    info:
      enabled: true

spring:
  application:
    name: example-spring-cloud-kubernetes-server-account

  cloud:
    kubernetes:
      client:
        namespace: ${NAMESPACE:default}
